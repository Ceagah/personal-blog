import React from "react";
import styled from "styled-components";

const Post: React.FC = () => {
  return (
    <S.Container>
      <h1>Post</h1>
    </S.Container>
  );
};

const S = {
  Container: styled.div``,
};

export default Post;
